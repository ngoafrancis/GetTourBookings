export const metadata = {
  title: "GetTourBookings",
  description: "Travel Tech & Consulting",
};

import "./globals.css";

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
